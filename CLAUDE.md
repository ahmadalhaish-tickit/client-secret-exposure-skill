# nextjs-secret-exposure

This plugin adds the `nextjs-secret-exposure` skill to Claude Code.

The skill activates automatically when you ask Claude to:
- Audit a Next.js project for leaked or exposed secrets
- Review `NEXT_PUBLIC_` environment variables
- Fix API keys exposed in the client bundle
- Understand the difference between CORS and Firebase App Check
